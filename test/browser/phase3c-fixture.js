// Retired compatibility entry point. Phase 3C's localStorage lease harness no
// longer matches hosted storage; use the hosted synthetic fixture instead.
export * from './hosted-storage-fixture.js';
